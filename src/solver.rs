use crate::searcher::Action;

#[derive(Debug, Clone)]
pub enum Solution {}

pub struct Solver;

impl Solver {
    pub fn new() -> Self {
        unimplemented!()
    }

    pub fn generate_solutions(&self, actions: Vec<Action>) -> Vec<Solution> {
        unimplemented!()
    }
}
